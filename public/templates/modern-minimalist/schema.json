{
  "template_slug": "modern-minimalist",
  "template_name": "Modern Minimalist",
  "description": "Clean, contemporary design with focus on the artwork",
  "version": "1.2.0",
  "author": "Artist Sites",
  "custom_fields": [
    {
      "key": "hero_tagline",
      "label": "Hero Section Tagline",
      "type": "text",
      "placeholder": "Your tagline here",
      "max_length": 100,
      "required": false,
      "help_text": "Short tagline displayed prominently on your homepage"
    },
    {
      "key": "hero_overlay",
      "label": "Hero Image Overlay",
      "type": "select",
      "description": "Add a translucent overlay to the hero image for better text readability",
      "required": false,
      "default_value": "off",
      "options": [
        { "value": "off", "label": "Off" },
        { "value": "on", "label": "On" }
      ]
    },
    {
      "key": "hero_overlay_color",
      "label": "Hero Overlay Color",
      "type": "select",
      "description": "Dark overlay for light text, light overlay for dark text",
      "required": false,
      "default_value": "dark",
      "options": [
        { "value": "dark", "label": "Dark (Black)" },
        { "value": "light", "label": "Light (White)" }
      ]
    },
    {
      "key": "gallery_layout",
      "label": "Gallery Layout Style",
      "type": "select",
      "description": "Choose how to display your artwork",
      "required": false,
      "default_value": "grid",
      "options": [
        { "value": "masonry", "label": "Masonry (Pinterest-style)" },
        { "value": "grid", "label": "Regular Grid" },
        { "value": "rows", "label": "Single Column Rows" }
      ]
    }
  ]
}
