(function(){'use strict';function init(){setupMousePerspective();console.log('Perspective Warp initialized')}function setupMousePerspective(){const grid=document.querySelector('.productsGrid, .addon-products-grid');if(!grid)return;grid.addEventListener('mousemove',function(e){const rect=this.getBoundingClientRect();const x=e.clientX-rect.left;const y=e.clientY-rect.top;const centerX=rect.width/2;const centerY=rect.height/2;const rotateY=(x-centerX)/centerX*10;const rotateX=(centerY-y)/centerY*10;this.style.transform=`rotateX(${rotateX}deg) rotateY(${rotateY}deg)`});grid.addEventListener('mouseleave',function(){this.style.transform='rotateX(0deg) rotateY(0deg)'})}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init)}else{init()}})();
