(function(){'use strict';function init(){setupScrollAnimations();setupKineticEffects();console.log('Kinetic Typography initialized')}function setupScrollAnimations(){const cards=document.querySelectorAll('.productCard, .addon-product-card');const observer=new IntersectionObserver(function(entries){entries.forEach((entry,index)=>{if(entry.isIntersecting){setTimeout(()=>{entry.target.classList.add('visible')},index*100)}})},{threshold:0.2});cards.forEach(card=>observer.observe(card))}function setupKineticEffects(){const artistName=document.querySelector('.artistName');if(!artistName)return;let lastScroll=0;window.addEventListener('scroll',function(){const currentScroll=window.pageYOffset;const delta=currentScroll-lastScroll;const skew=Math.min(Math.max(delta*0.5,-10),10);artistName.style.transform=`skewY(${skew}deg)`;lastScroll=currentScroll},{passive:true})}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init)}else{init()}})();
