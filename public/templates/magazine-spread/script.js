(function(){'use strict';function init(){setupParallaxImages();console.log('Magazine Spread initialized')}function setupParallaxImages(){const images=document.querySelectorAll('.productImage, .addon-product-image-wrapper');window.addEventListener('scroll',function(){images.forEach(img=>{const rect=img.getBoundingClientRect();const scrolled=window.pageYOffset;if(rect.top<window.innerHeight&&rect.bottom>0){const speed=0.1;const yPos=-(scrolled*speed);img.style.transform=`translateY(${yPos}px)`}})},{passive:true})}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',init)}else{init()}})();
