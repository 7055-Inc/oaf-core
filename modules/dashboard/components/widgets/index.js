/**
 * Dashboard Widget Components
 * Widget grid and individual widget items
 */

export { default as WidgetGrid } from './WidgetGrid';
export { default as WidgetRenderer } from './WidgetRenderer';

// Widget items are accessed via WidgetRenderer, not directly exported
