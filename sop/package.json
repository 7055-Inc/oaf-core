{
  "name": "sop-catalog",
  "version": "1.0.0",
  "description": "SOP Catalog — contained process (own DB, server, routes)",
  "main": "src/server.js",
  "scripts": {
    "build": "npm run build:sop-editor --prefix ..",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
