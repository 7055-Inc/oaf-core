/**
 * Events Module
 * Handles event management including creation, editing, applications, and add-ons
 */

const routes = require('./routes');
const services = require('./services');

module.exports = {
  routes,
  services
};
