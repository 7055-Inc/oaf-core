/**
 * Users Middleware
 * Re-exports user-specific middleware
 */

// TODO: Add user-specific middleware as needed
// const requireProfileComplete = require('./requireProfileComplete');

module.exports = {
  // requireProfileComplete,
};
